# ToDo

Wiped for a fresh start. The operator declares the next topic in a new chat; the agent then cold-starts per `README.md` (read `MASTER.md` in full, `node tools/orient.ts orient`, deploy bounded agents). Durable machine state lives in `MASTER.md`, not here.

| Goal | Sentence | Done |
|---|---|---|
| New topic | Set by the operator in the new chat. | ☐ |
