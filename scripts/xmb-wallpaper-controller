#!/usr/bin/env bash
# Deliberately disabled: the shell/xprop crossfade failed live (X-140/X-142).
# Keep the proven one-decode launcher until a single-renderer transition path
# can preserve loop continuity and coalesce rapid viewport changes.
echo "xmb-wallpaper-controller: disabled after failed live crossfade" >&2
echo "use: XMB_USE_GEOMETRY=1 xmb-wallpaper ROLE background" >&2
exit 2
