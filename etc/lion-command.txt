LION-SSD | gate 1 of 1 | relay today's Repair report, then stop

STATE: PR #76 merged to main at 2026-09-11 05:27 UTC (c93cac2). The relay page and
both zips are on main. If this box shows text, the page is reaching the repo fine.

DO NOW (clicks only, nothing to type):
1. Click Burn on this page. Either zip name works and they are byte-identical:
   lion-mirror.zip (what da.gd/lmz resolves to) or lion-mirror2.zip.
   Each contains lion-mirror.command (exec bit) and lion.html (this page).
2. Unzip on the Desktop, then double-click lion.html to keep using it offline.
3. In the Log box choose ~/Desktop/lion-mirror.txt. One pick sends the whole report
   as text/plain, the same verdict lines as a ?log= query, and the file as before.
4. Reply here with one word: sent

IF BURN STILL GIVES A ONE-FILE ZIP: the jsDelivr branch cache has not refreshed
(it holds a branch path about 12h and ignores cache-buster query strings at the edge).
Do not wait on it - open lion-mirror2.zip from the Burn link on this page instead,
which is a fresh path that cannot be cached.

ALREADY IN THE INBOX BUT UNREADABLE BY DESIGN OF THE OLD PAGE:
14:02 7199B, 14:25 4185B, 14:32 4185B, 14:33 7564B, 14:46 7564B, 14:48 4826B,
14:59 4930B, 15:02 4902B (uuid 3dc2ab98 = lastVerifiedBless), 17:16 4902B.
Those were multipart uploads, so the agent can see the envelope and not the bytes.

HOLD until the agent reads the summary:
- no Restart, no Option-boot, no second Repair
- never erase Lion SSD Base or start disk clone, never remirror Bay 4
- never run lion-installer-date-fix.command, never visit da.gd/lionfix or da.gd/lup

RELEASED ONLY BY THE NEXT VERSION OF THIS FILE:
Option at the chime, choose "Mac OS X Install ESD", install onto "start disk clone"
(Bay 3 MX500), do not erase, then create the new admin after the first Lion boot.
