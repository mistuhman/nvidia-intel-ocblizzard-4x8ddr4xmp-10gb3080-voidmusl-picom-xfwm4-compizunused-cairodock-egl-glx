LION-SSD | gate 1 of 1 | relay today's Repair report, then stop

STATE: current session branch carries the reproducible relay page and bundle.
Type only the short da.gd links below from the phone into Arctic Fox; never use
TinyURL, da.gd/lmz, or da.gd/lpg for this session.

DO NOW (clicks only):
1. Type da.gd/lionrelay into Arctic Fox. It opens the current relay page.
2. The page's Burn button goes to da.gd/lionzip. If downloading directly, type
   da.gd/lionzip; it contains lion-mirror.command and lion.html.
3. If you downloaded the bundle, unzip on the Desktop and double-click lion.html
   so the page also works offline.
4. In the Log box choose ~/Desktop/lion-mirror.txt. One pick sends the whole report
   as text/plain, the same verdict lines as a query, and the file as before.
5. Reply here with one word: sent

WHY THESE LINKS: da.gd slugs are write-once and the exact destinations are recorded
in docs/lion-workflow.json. node tools/lion-status.ts prints them; its selftest
checks both short links and rejects TinyURL. The page itself is carried by the
current branch so the Commands panel needs no typed long URL.

IN THE INBOX BUT UNREADABLE BY THE OLD PAGE'S DESIGN:
14:02 7199B, 14:25 4185B, 14:32 4185B, 14:33 7564B, 14:46 7564B, 14:48 4826B,
14:59 4930B, 15:02 4902B (uuid 3dc2ab98 = lastVerifiedBless), 17:16 4902B.
Those were multipart uploads: the agent sees the envelope, never the bytes.

HOLD until the agent reads the summary:
- no Restart, no Option-boot, no second Repair
- never erase Lion SSD Base or start disk clone, never remirror Bay 4
- never run lion-installer-date-fix.command, never visit da.gd/lionfix or da.gd/lup

RELEASED ONLY BY THE NEXT VERSION OF THIS FILE:
Option at the chime, choose "Mac OS X Install ESD", install onto "start disk clone"
(Bay 3 MX500), do not erase, then create the new admin after the first Lion boot.
