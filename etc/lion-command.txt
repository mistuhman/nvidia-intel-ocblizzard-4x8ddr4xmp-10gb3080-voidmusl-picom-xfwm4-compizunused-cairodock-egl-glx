LION-SSD | gate 1 of 1 | relay today's Repair report, then stop

STATE: PR #76 and #77 merged to main. The relay page, both zips, the imaging tool
and every art receipt are on main. If this box shows text, the page reaches the
repo and nothing on the Mac has to be typed again.

DO NOW (clicks only):
1. Open the page (bookmark it or use the phone-minted da.gd slug).
2. Click Burn. It fetches lion-mirror2.zip pinned to tag lion-relay-1, which is
   always moved to the newest relay build. Either zip name is byte-identical.
   The zip holds lion-mirror.command (exec bit) and lion.html (this page).
3. Unzip on the Desktop, double-click lion.html so the page also works offline.
4. In the Log box choose ~/Desktop/lion-mirror.txt. One pick sends the whole report
   as text/plain, the same verdict lines as a ?log= query, and the file as before.
5. Reply here with one word: sent

WHY THE TAG AND NOT A BRANCH: jsDelivr returns HTTP 500 for @main on this repo and
caches a branch path for ~12h while ignoring cache-buster query strings at the edge.
A tag ref resolves instantly, so the bundle link is stable. The Commands panel above
reads raw.githubusercontent instead (branch first, then main, ~5 min max-age), which
is why this text can change without waiting on any cache.

IN THE INBOX BUT UNREADABLE BY THE OLD PAGE'S DESIGN:
14:02 7199B, 14:25 4185B, 14:32 4185B, 14:33 7564B, 14:46 7564B, 14:48 4826B,
14:59 4930B, 15:02 4902B (uuid 3dc2ab98 = lastVerifiedBless), 17:16 4902B.
Those were multipart uploads: the agent sees the envelope, never the bytes.

HOLD until the agent reads the summary:
- no Restart, no Option-boot, no second Repair
- never erase Lion SSD Base or start disk clone, never remirror Bay 4
- never run lion-installer-date-fix.command, never visit da.gd/lionfix or da.gd/lup

RELEASED ONLY BY THE NEXT VERSION OF THIS FILE:
Option at the chime, choose "Mac OS X Install ESD", install onto "start disk clone"
(Bay 3 MX500), do not erase, then create the new admin after the first Lion boot.

SHORT LINK (typeable on the Mac) - mint it once from the phone, which can paste:
  go to da.gd, set Short Code to: lpg2
  paste this as the long URL:
  https://htmlpreview.github.io/?https://github.com/mistuhman/nvidia-intel-ocblizzard-4x8ddr4xmp-10gb3080-voidmusl-picom-xfwm4-compizunused-cairodock-egl-glx/blob/lion-relay-1/lion.html
  then type da.gd/lpg2 on the Mac, forever. It is pinned to the TAG, and the agent
  moves that tag to each new relay build, so the short link never goes stale and
  never needs re-minting. da.gd is write-once: one slug, no edits, that is why this
  tag indirection exists. The agent cannot mint it (no shortener egress from the
  sandbox) - only a browser can.
