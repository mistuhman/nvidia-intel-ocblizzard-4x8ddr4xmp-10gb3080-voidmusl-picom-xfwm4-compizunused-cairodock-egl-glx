LION-ONE | 2026-09-16h | current one link da.gd/sQ7bEo

RECEIPTS READ: three text burns received 2026-09-16 16:24:20-39 (server time).
- passthrough-CONTEXT.txt: readable companion (not a runtime test).
- COMPILERCTX1: v2 CORPUS_SHA=MATCH; complete COMPILERCTX1_DONE.
- LIONONE1: Lion 10.7.5, Arctic Fox 47.3, GTX 285, two 1080p displays.

COMPILE_GATE=HOLD. No repeat burn is needed for these three files.
The v2 converter read the wrong Babel field and emitted BigInt("undefined").
Corrected in v3; core-js still DOES NOT supply BigInt on an engine without it - v4 ships its own verified BigInt + regex compat layer (compat.js).
Unverified BigInt / deferred-regex rewrites now block transpile and receipt.
Instruction export is context delivery, NOT an app build or runtime PASS.
The installed GitHub workflow is ACTIVE with zero runs, but is the OLD copy.
Do not Run workflow yet. Corrected source is ci/workflows/mac-es5-passthrough.yml;
operator installation of that source alone will NOT solve the runtime blockers.
Next single task: verify an actual BigInt/regex compatibility implementation.
New context downloads carry v5 d94486672b9a and explicitly report this HOLD.

STORAGE HOLD: root is start disk clone (/dev/disk3s2 at receipt time).
Old lion-wipe-harden.txt directions are SUSPENDED; no erase is armed.
Protect the current boot volume, Lion SSD Base, Raid X and every RAID member.
Disk numbers are observations, NEVER reusable erase targets; bay IDs unknown.
Firewall reports disabled; no hardening, AirPort reset or MicPort completion
is inferred from this inventory. Those tasks remain queued, not released.

The One page still accepts text reports: choose the txt, press Burn.
No merge, no pull request is needed to send a report straight to the model.
PR merges are OPERATOR-ONLY. Agent reminds and never merges.
