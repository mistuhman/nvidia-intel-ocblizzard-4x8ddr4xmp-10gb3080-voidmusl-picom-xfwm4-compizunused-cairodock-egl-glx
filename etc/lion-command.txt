LION-SSD | gate 1 of 1 | capture the ESD boot handoff as a root-level text report

STATE: the current session branch carries the reproducible relay page and bundle.
Use only these short links from the phone into Arctic Fox; never use TinyURL,
da.gd/lmz, or da.gd/lpg for this session:
  da.gd/lionrelay  current readable-report relay page
  da.gd/lionzip    current three-file bundle

CURRENT BUNDLE:
- lion-mirror.command      current ESD Repair helper; do not run it again now
- lion-boot-log.command    safe post-fallback boot-handoff collector
- lion.html                relay page

WHY THIS LOG: the EFI/boot.efi screen runs before Snow Leopard's shell and cannot
redirect its console text into a file after the fallback. The collector therefore
records the operator's observed screen result plus read-only NVRAM boot keys, dmesg,
system.log, disk inventory, ESD boot files, and bless metadata. It does not repair,
bless, mount, erase, restore, set the clock, or change NVRAM.

REPORT ALREADY READ BY THE AGENT:
2026-09-11 16:10:02 PDT | webhook uuid 2ad981cf | 4902 bytes | 124 lines
- Repair confirmed; Graphics Mode removed; nvram Graphics Mode absent
- bless OK; volume-root /Volumes/Mac OS X Install ESD/boot.efi; finderinfo 2078
- ESD boot repaired. No disk was erased.

RELEASED OPTION-BOOT RESULT:
The operator selected Mac OS X Install ESD. The screen showed a prohibitory
symbol briefly, then a white-background Apple logo/loading sign, then returned
to the Snow Leopard login. The installer GUI was not reached.

DO NOW — ONE DIAGNOSTIC/LOGGING WAVE, NO REBOOT:
1. Type da.gd/lionzip into Arctic Fox and download the current bundle.
2. Unzip it on the Desktop. Double-click lion-boot-log.command only.
3. In its one screen-result dialog choose "Prohibitory then fallback".
4. The command writes the canonical root-level file /lion-boot-log.txt and a Finder-
   friendly duplicate ~/Desktop/lion-boot-log.txt. It only reads the disks/logs/NVRAM;
   it does not change them.
5. Open da.gd/lionrelay and choose the Desktop copy lion-boot-log.txt. Wait for
   burned: text=ok query=ok, then reply here with one word: burned

DO NOT:
- double-click the visible lion-installer-boot-audit or lion-installer-date-fix files
- run the withdrawn injector/date-fix path, lion-mirror.command, or another Repair
- remirror Bay 4 or erase Lion SSD Base / start disk clone
- perform another reboot until the root-level report is read
- visit da.gd/lionfix or da.gd/lup

AFTER THE AGENT READS BOOTLOG1:
The next physical action will be chosen from the text receipt. Lion installation
remains pending; the Bay 3 start disk clone remains preserved.

The previous readable receipt was: burned: text=ok query=ok.
