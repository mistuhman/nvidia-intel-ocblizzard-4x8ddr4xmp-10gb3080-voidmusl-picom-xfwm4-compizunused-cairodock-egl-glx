https://da.gd/absolution
ABSOLUTION - rollout R1 - branch arena/01a0ad71-nvidia-intel-ocblizzard-4x8ddr - PR #91.
ONE NAME everywhere: command absolution.command, page absolution.html, bundle
absolution.zip, log tag ABSOLUTION1 / ABSOLUTION1_DONE. Say "absolution" in chat.
Wave: clear + harden the Lion SSD WITHOUT wiping drives (operator 2026-09-17).
Step 1 (now): open the link in Arctic Fox, download absolution.command, double-click.
Step 2: attach ~/Desktop/absolution.txt on the same page, press Burn, say "burned".
The probe is READ-ONLY: no sudo, no erase, no move, no delete, no network.
App removal (later, separate, CONFIRM-gated) is a REVERSIBLE MOVE to
/Users/Shared/lion-quarantine/<stamp>/ - never rm, never eraseDisk.
KEEP: CandyBar, Flavours, AirPort, Arctic Fox, iTunes, Audacity/AU Lab/GarageBand/
Logic, Utilities, plus themes/icons/Downloads. Apple bundles protected by class.
STORAGE HOLD stands: boot volume is "start disk clone"; old wipe wave SUSPENDED,
its CONFIRM line is never reused. Protect boot + all RAID members.
PERSISTENT BY POINTER: the page reads ABSOLUTION.json at load (main, then branch) and
retargets itself, so this ONE link serves every future rollout. Never mint a second slug.
Next wave = edit ROLLOUT/BRANCH/PR in tools/absolution.ts, run emit, push. Link unchanged.
Regenerate byte-identically: node tools/absolution.ts emit ; links ; parse FILE
Legacy da.gd/sQ7bEo and da.gd/lionone burn to the same inbox but are frozen pages.
Withdrawn: da.gd/lionfix, da.gd/lup, TinyURL.
